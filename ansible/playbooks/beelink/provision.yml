- name: Provision host
  hosts: beelink
  remote_user: root
  gather_facts: no
  tasks:
    - import_role:
        name: docker
      tags: [os]
    - import_role:
        name: pihole
      tags: [pihole]
  vars:
    - runtime_user: emcniece

