apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plexpy-config
  namespace: media
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 500Mi
  storageClassName: csi-cephfs-sc
