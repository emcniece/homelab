apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plexpy-config
  namespace: media
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 5Gi
  storageClassName: csi-rbd-sc
