apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: immich-tls
  namespace: immich
spec:
  dnsNames:
  - immich.lab.emc2.build
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: immich-tls
