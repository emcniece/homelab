apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scrutiny-web-config
  namespace: scrutiny
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: csi-rbd-sc
