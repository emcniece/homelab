apiVersion: v1
kind: Service
metadata:
  name: scrutiny-web
  namespace: scrutiny
spec:
  selector:
    app.kubernetes.io/name: scrutiny
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 8080
      targetPort: 8080
