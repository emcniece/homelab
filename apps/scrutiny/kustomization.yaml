apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: scrutiny

resources:
  - namespace.yaml
  - influxdb-pvc.yaml
  - influxdb-deployment.yaml
  - influxdb-service.yaml
  - web-pvc.yaml
  - web-configmap.yaml
  - web-deployment.yaml
  - web-service.yaml
  - ingress.yaml
  # Secret is created out-of-band — never commit it:
  #   scrutiny-influxdb → see secret.yaml.example
