apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calibre-downloader-config
  namespace: media
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 1Gi
  storageClassName: csi-cephfs-sc
