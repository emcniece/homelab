apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kdds-wp
  namespace: kdds
spec:
  storageClassName: csi-cephfs-sc
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
