# ---
# ORIGINAL:
# apiVersion: traefik.io/v1alpha1
# kind: IngressRoute
# metadata:
#   name: homepage
#   namespace: default
#   labels:
#     app.kubernetes.io/name: homepage
#   annotations: 
#     kubernetes.io/ingress.class: traefik-external
#     gethomepage.dev/description: Dynamically Detected Homepage
#     gethomepage.dev/enabled: "true"
#     gethomepage.dev/group: Cluster Management
#     gethomepage.dev/icon: homepage.png
#     gethomepage.dev/name: Homepage
# spec:
#   entryPoints:
#     - websecure
#   routes:
#     - match: Host(`homepage.lab.emc2.build`)
#       kind: Rule
#       services:
#         - name: homepage
#           port: 3000
#       middlewares:
#         - name: default-headers
#           namespace: default
#   # tls:
#   #   # This must match the certificate secretName!
#   #   secretName: lab-emc2-build-tls


---
# UPDATE DEC 13, copied from heimdall ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    gethomepage.dev/description: Dynamically Detected Homepage
    gethomepage.dev/enabled: "false"
    gethomepage.dev/group: Cluster Management
    gethomepage.dev/icon: homepage.png
    gethomepage.dev/name: Homepage
  name: homepage
  namespace: default
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - homepage.lab.emc2.build
    secretName: homepage-letsencrypt-tls
  rules:
  - host: homepage.lab.emc2.build
    http:
      paths:
      - backend:
          service:
            name: homepage
            port:
              number: 3000
        path: /
        pathType: Prefix
