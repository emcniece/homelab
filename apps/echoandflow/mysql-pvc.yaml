apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: echoandflow-mysql
  namespace: echoandflow
spec:
  storageClassName: csi-cephfs-sc
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
