apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-staging
  namespace: media
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 20Ti
  storageClassName: csi-cephfs-sc

